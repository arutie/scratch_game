package org.sg.models.symbol;

import org.sg.models.Reward;
import org.sg.models.Symbol;

public class BonusSymbol extends Symbol {
    public BonusSymbol(String name, Reward reward) {
        super(name, reward);
    }
}
