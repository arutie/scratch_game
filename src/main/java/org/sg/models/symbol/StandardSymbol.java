package org.sg.models.symbol;

import org.sg.models.Reward;
import org.sg.models.Symbol;

public class StandardSymbol extends Symbol {
    public StandardSymbol(String name, Reward reward) {
        super(name, reward);
    }
}
